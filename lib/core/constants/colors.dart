import 'package:flutter/material.dart';

class AppColors {
  // Updated primary colors
  static const Color primary = Color(0xFF80D0C7); // Light teal as the new primary color
  static const Color primaryContainer = Color(0xFF66B3A6); // Slightly darker teal
  static const Color onPrimary = Color(0xFFFFFFFF); // White for contrast on primary
  static const Color onPrimaryContainer = Color(0xFF0F4560); // Darker blue for contrast

  // Secondary colors related to primary
  static const Color secondary = Color(0xFF13547A); // Deep blue as the secondary color
  static const Color secondaryContainer = Color(0xFF0F4560); // Slightly darker blue
  static const Color onSecondary = Color(0xFFFFFFFF); // White for contrast on secondary
  static const Color onSecondaryContainer = Color(0xFF80D0C7); // Light teal for contrast

  // Background and surface colors related to primary
  static const Color background = Color(0xFFE0F7F4); // Light teal tint for background
  static const Color onBackground = Color(0xFF13547A); // Deep blue for text on background
  static const Color surface = Color(0xFFFFFFFF); // White for surface
  static const Color onSurface = Color(0xFF13547A); // Deep blue for text on surface

  // Surface variant colors
  static const Color surfaceVariant = Color(0xFFB2DFDB); // Light teal variant for surface
  static const Color onSurfaceVariant = Color(0xFF004D40); // Dark teal for text on surface variant

  // Error colors
  static const Color error = Color(0xFFD6394C);
  static const Color errorContainer = Color(0xFFFFDAD6);
  static const Color onError = Color(0xFFFFFFFF);
  static const Color onErrorContainer = Color(0xFF410002);

  // Outline colors
  static const Color outline = Color(0xFF80D0C7); // Light teal for outlines
  static const Color outlineVariant = Color(0xFF66B3A6); // Slightly darker teal

  // Shadow color
  static const Color shadow = Color(0xFF000000);

  // Scrim color
  static const Color scrim = Color(0xFF000000);

  // Custom colors for specific use cases
  static const Color upGreen = Color(0xFF03A66D);
  static const Color downRed = Color(0xFFD6394C);
  static const Color inactiveGray = Color(0xFF9E9E9E);

  // Dark theme specific colors
  static const Color darkSurface = Color(0xFF2C2C2C);
  static const Color darkSurfaceContainer = Color(0xFF3C3C3C);
  static const Color darkBackground = Color(0xFF1E1E1E);

  // Trading chart colors
  static const Color chartGreen = Color(0xFF4CAF50);
  static const Color chartRed = Color(0xFFD32F2F);
  static const Color chartAmber = Color(0xFFFFC107);

  // Shadow colors
  static const Color shadowLight = Color(0xFF000000);
}
